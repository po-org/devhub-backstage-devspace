{
  "name": "@red-hat-developer-hub/webhook-backend-plugin",
  "version": "0.1.0",
  "description": "Webhook backend plugin for Red Hat Developer Hub with user and group notifications",
  "main": "dist/index.cjs.js",
  "types": "dist/index.d.ts",
  "license": "Apache-2.0",
  "backstage": {
    "role": "backend-plugin",
    "pluginId": "webhook"
  },
  "scripts": {
    "build": "backstage-cli package build",
    "lint": "backstage-cli package lint",
    "test": "backstage-cli package test",
    "clean": "backstage-cli package clean"
  },
  "dependencies": {
    "@backstage/backend-plugin-api": "^0.7.0",
    "@backstage/plugin-notifications-node": "^0.6.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@backstage/cli": "^0.26.11",
    "@types/express": "^4.17.17"
  },
  "files": [
    "dist",
    "config.d.ts"
  ]
}
